{
  "name": "upload-media-worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "dev": "bun --hot src/index.ts",
    "minify": "bun run typecheck && bun build src/index.ts --target bun --outdir .dist --minify",
    "prod": "bun run --bun .dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "rabbitmq-client": "^5.0.5"
  }
}
